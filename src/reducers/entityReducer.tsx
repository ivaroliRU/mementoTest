var initialState = [
    {
        Title: "Test",
        Subtitle: "Test test"
    }
];

export default function(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}