import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
    },
    alertTitle: {
        textAlign: 'center',
        fontSize: 20,
        marginTop: 150,
        color: 'lightgrey'
    },
    alertSubtitle: {
        textAlign: 'center',
        color: 'lightgrey'
    }
});

export default styles;